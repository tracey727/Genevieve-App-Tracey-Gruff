import { neon } from '@neondatabase/serverless';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type','application/json; charset=utf-8');const base={ok:true,service:'genevieve-dog-park',appStage:'Stage 3',interfaceLayer:'source-of-truth-2026-08-18',stageHistory:['Stage 1 foundation','Stage 2 UX/UI'],foundationStructure:'first-structure-2026-08-18'};if(!process.env.DATABASE_URL)return res.status(200).json({...base,database:'not-configured',databaseStage:null,status:'structure-ready-database-binding-pending'});try{const sql=neon(process.env.DATABASE_URL),a=await sql`SELECT structure_version,stage_label,status FROM stage1.app_foundation WHERE id=1`,b=await sql`SELECT structure_version,stage_label,status FROM stage3.build_state WHERE singleton_id=1`,f=a[0]??null,s=b[0]??null,ready=Boolean(f&&s);return res.status(ready?200:503).json({...base,ok:ready,database:ready?'connected':'stage-foundation-missing',foundationStage:f?.stage_label??null,foundationStatus:f?.status??null,databaseStage:s?.stage_label??null,databaseStatus:s?.status??null,databaseStructure:s?.structure_version??null,status:ready?'ready':'stage-foundation-missing'});}catch(e){console.error('Database Stage 3 health check failed',e instanceof Error?e.message:'unknown-error');return res.status(503).json({...base,ok:false,database:'unavailable',databaseStage:null,status:'database-unavailable'});}}
